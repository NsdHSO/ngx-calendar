import { TimePeriodPipe } from './time-perriod.pipe';

describe('TimePerriodPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
