import { LineHorizontalDirective } from './line-horizontal.directive';

describe('LineHorizontalDirective', () => {
  it('should create an instance', () => {
    const directive = new LineHorizontalDirective();
    expect(directive).toBeTruthy();
  });
});
